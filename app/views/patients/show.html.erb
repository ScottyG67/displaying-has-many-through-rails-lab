<h1><%=@patient.name%></h1>

<table>
    <tr>
        <th>Appointment</th>
        <th>Doctor</th>
    </tr>
<% @patient.appointments.each do |appointment| %>
    <tr>
        <td><%= appointment.pretty_date_time %></td>
        <td><%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %></td>
        
    </tr>
<% end %>
</table>